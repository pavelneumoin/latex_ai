# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.fdb_latexmk
*.fls
*.synctex.gz

# Node
node_modules/
.next/
out/
.env
.env.local
.env*.local
npm-debug.log*

# TS build cache
*.tsbuildinfo

# Yandex.Disk duplicate files
* (1).*
* (2).*
* (3).*

# Prisma — local dev SQLite + auto-generated client
frontend/prisma/*.db
frontend/prisma/*.db-journal
frontend/prisma/migrations/dev.db*

# Storage — uploaded files, generated PDFs, logos
frontend/storage/uploads/*
frontend/storage/worksheets/*
frontend/storage/logos/*
frontend/storage/_temp_uploads/*
!frontend/storage/uploads/.gitkeep
!frontend/storage/worksheets/.gitkeep
!frontend/storage/logos/.gitkeep
!frontend/storage/_temp_uploads/.gitkeep

# Python
__pycache__/
*.pyc
.venv/
venv/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Build/output of CLI sprint — keep PDFs and JSONs but not aux/log
cli/output/*/T*.aux
cli/output/*/T*.log
cli/output/*/T*.out
cli/output/*/T*.toc

# secrets
*.key
*.pem
.anthropic_key

# Bank of tasks — built from Lessons/vault, copied to server via scp (too big for git)
frontend/data/bank.json
