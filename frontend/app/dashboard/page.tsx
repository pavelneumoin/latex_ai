import { StubPage } from "../_components/StubPage";

export default function DashboardPage() {
  return (
    <StubPage
      title="Дашборд"
      description="После входа здесь будут последние созданные листы, статистика за месяц и быстрая ссылка на новую генерацию. Пока — заглушка."
    />
  );
}
