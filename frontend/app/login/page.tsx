import { StubPage } from "../_components/StubPage";

export default function LoginPage() {
  return (
    <StubPage
      title="Вход через VK"
      description="На MVP — авторизация через VK ID, ручное одобрение первых пользователей. В разработке. Пока можешь зайти на «Создать лист» — генератор работает без логина."
    />
  );
}
